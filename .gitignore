# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# CBS API spike (issue #5) — never commit the token or real league data
/spikes/cbs-api/.env
/spikes/cbs-api/output/
/spikes/cbs-api/*.har

# FantasyPros API spike (issue #7) — never commit the API key or pulled data
/spikes/fantasypros-api/.env
/spikes/fantasypros-api/output/

# Raw snapshot archive (issue #10) — real league + third-party data and operational
# logs, never committed. tools/archive/capture.mjs re-creates this folder each run.
/data/
