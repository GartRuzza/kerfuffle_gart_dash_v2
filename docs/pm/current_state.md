# Current State — Gart Dash

> **How to use this doc**
> **Owner:** Claude Code (the implementing agent), verified by the product owner.
> **Update when:** At the end of **every** build cycle, before reporting the work complete. This is not optional — an out-of-date current_state is worse than no current_state, because it is trusted.
> **This doc contains:** Only what exists in the code **right now**.
> **This doc never contains:** Plans, intentions, or anything phrased as "will." If it is not built, it does not get described here as if it were. Plans live in [`roadmap.md`](roadmap.md).
>
> **Read this doc before any planning or building.** It is the grounding doc — it exists to stop us from assuming a feature is built when it is not.

**Last updated:** 2026-08-26 · **Updated by:** Claude Code (**projection engine core, issue #18**; historical data storage, #17; storage schema + ingestion, #12; superflex display board, D-12) · **Reflects:** the normalized SQLite store + real-data table (issue #12), on the raw archive (#10), field profile (#11), and the discovery spikes (#5, #7), the historical CBS 2024/25 stats + 2025 salaries + TRUFFLE reference (issue #17), **and now the KERFUFFLE projection engine core (issue #18): FantasyPros projections + estimated first downs → Kerf projected points → real Kerf overall/positional ranks + tiers in the table.** The **projection** layer is built; the **valuation** layer (dollars/ceilings/Edge, #20) and the **backtest** (#19) are not — dollars still render "—" (see [D-13](../decision_log.md)).

---

## At a glance

**The table now shows the real league, ranked the KERFUFFLE way.** The pipeline is complete through the projection engine: `npm run archive` saves verbatim snapshots (#10), `npm run ingest` normalizes them + the FantasyPros projections (#12/#18), `npm run ingest:historical` loads the 2024/25 stat history (#17), and **`npm run engine` (#18) turns FantasyPros' projected stat lines — plus an *estimated* first-down component from our own league history — into KERFUFFLE projected points, then real Kerf overall/positional ranks + tiers.** The app renders all of it: real rosters, salaries, contracts, statuses, free agents, FantasyPros rankings/tiers, and now **Kerf ranks/tiers with quarterbacks correctly at the top** (Josh Allen #1 overall). What does **not** exist yet: the **dollar layer** (Kerf Value, Market Value, Edge, ceiling suggestions all still "—"), the **backtest** (#19, the gate before dollars), defenses' Kerf numbers, and the three lenses.

- **Built** — works end to end, in the product, usable by a real user today.
- **Partial** — some of it exists, but a real user cannot rely on it yet. The gap must be named in the section below.
- **Not built** — no usable implementation exists, even if code has been started.

| Feature / capability | Status | Notes |
| --- | --- | --- |
| Player table (on real data) | **Built** | Issue #12 (roadmap Now #6). One dark screen rendering the **real KERFUFFLE league** from the local SQLite store via the single data-access module: all 12 rosters (salary, contract, status incl. Practice Squad), the FantasyPros-derived free-agent pool, real ECR/dynasty ranks from the **superflex** boards (D-12) and **real tier bands**, DST as a real position. **Now also renders the engine's Kerf Ovr/Pos ranks + tiers and KERFUFFLE-scored Proj Points (issue #18).** Dollar columns still show **"—" honestly** (see Partial below). Banner: "League data as of \<date\>". All prototype interactions carried over unchanged. |
| Storage + ingestion (normalized SQLite) | **Built** | Issue #12. `npm run ingest` (tools/ingest/) parses every archived run into `data/gart-dash.sqlite` (better-sqlite3, migrations `db/migrations/001`–`003`): `pull`, `fantasy_team`, `player`, `contract` (snapshot w/ dead-cap + Practice-Squad modelling per D-11), `league_transaction` (60 real transactions), `market_ranking` (11 boards/pull), `scoring_rule` (24 parsed rules) + the flat `board` view. **Constitution invariants validated loudly** (12 teams, id-or-dead-cap, cap ≤ $500, contract 1–4, header-name mapping, one-roster-per-player); one transaction per run (failure = full rollback); **idempotent re-ingest proven**; every row carries `pull_id` + `fetched_at` lineage to its raw snapshot. |
| Raw snapshot archive (CBS + FantasyPros) | **Built** | Operator tool (issue #10, roadmap #4; **extended in #12**: the paginated transaction log is now fully captured — print-all view + every page — and a dynasty-STD probe added). `npm run archive` saves every fetched response verbatim into a new dated folder under `data/raw/` with a per-run manifest. Append-only; manual, ~weekly; `npm run archive:check-cookie` first. `data/` and credentials git-ignored. |
| Source field profile (CBS + FantasyPros) | **Built** | Discovery tool (issue #11, roadmap #5). `npm run profile` emits the committed, shape-only field profile + the `/rules` scoring config to [`docs/profiles/`](../profiles/). Unchanged by #12 (its parsers are reused by ingestion). |
| Valuation engine — **projection core** | **Built** | Issue [#18](https://github.com/GartRuzza/kerfuffle_gart_dash_v2/issues/18) (roadmap #8). `npm run engine` (`tools/engine/`) + migration `005` (`projection_source` input, `engine_run` + `projection` derived). Translates FantasyPros projected stat lines → **KERFUFFLE projected points incl. an estimated first-down component** — **per-player** rush-FD-per-carry / rec-FD-per-reception rates from 2024+2025, **shrunk toward the position rate by sample size** so a genuine standout is credited while rookies/thin samples fall back to average (D-14; owner) → **Kerf overall/positional ranks + gap-based tiers** (Jenks natural breaks calibrated to FantasyPros' tier counts). Reuses #17's validated scoring. **520 players scored; QBs correctly premium (Josh Allen #1 overall); Kyren Williams boosted above the RB average on his real first-down rate.** Defenses excluded (can't score defense from the offensive feed — owner). No dollars. |
| Valuation engine — **dollars/backtest** | Not built | Remaining engine stages: [#19](https://github.com/GartRuzza/kerfuffle_gart_dash_v2/issues/19) backtest gate → [#20](https://github.com/GartRuzza/kerfuffle_gart_dash_v2/issues/20) VORP dollars/ceilings/Edge (last-starter replacement QB24/RB~34/WR~34/TE~17/DST12, single-season, two ceilings — [D-13](../decision_log.md)). The backtest gates the dollar layer: prove the Kerf re-rank beats ECR before pricing. Kerf Value / Market Value / Edge / Ceiling seed stay "—" until then. |
| Historical data (CBS 2024/25 stats, 2025 salaries, TRUFFLE auction) | **Built** | Issue [#17](https://github.com/GartRuzza/kerfuffle_gart_dash_v2/issues/17) (roadmap #7). Migration `004` + `npm run ingest:historical` load three tables from `data/historical/`: **`player_season_stats`** (2024 & 2025 CBS stat lines — rush/rec/pass **first downs** joined with full volume + FPTS, offense only, 864 universe players/season), **`contract_history`** (KERFUFFLE **2025** salaries — the only authoritative year; 223/234 matched), **`auction_result`** (TRUFFLE 2026, 69 rows, `is_reference=1`, **read by no consumer** — [D-15](../decision_log.md), test-guarded). Name→id matching with loud unmatched reporting; **scoring cross-check passes** (recomputed KERFUFFLE points match CBS FPTS Total within ~0.5 pt for ~96% of players). These are the engine's first-down rates, the backtest's ground truth, and the price-curve signal. |
| Backtest | Not built | — |
| Auction prep lens | Not built | The Auction Prep *view* exists (real FAs, real salaries) but ceilings have no engine suggestions and don't persist. |
| Waiver additions | Not built | — |
| Trade evaluation/construction | Not built | — |

## Partially built — what exactly is missing

**The table's *dollar* columns are deliberately empty — that is the named gap, not a Partial status.** As of issue #18 the **Kerf ranks/tiers are real**; what remains "—" is the money. What renders as "—" and why:

- **Kerf Value, Market Value, Edge** — the *dollar* outputs, from the valuation layer (#20), which does not exist yet (gated behind the backtest #19). Nothing is invented to fill them.
- **Ceiling** starts blank — it pre-fills from Kerf Value by design, which is null until the dollar layer lands. Still editable, still session-only.
- **Defenses' Kerf columns** — DST is excluded from the projection engine (its scoring can't be derived from the offensive projection feed — owner, 2026-08-26); defenses keep their market positional rank/tier and show "—" for Kerf.
- **Kerf ranks/tiers + Proj Points are now filled** for projected offense (incl. free agents) by the engine (#18) — they are no longer part of this gap.

The prototype-era description below is kept for history of what the UI shell does:

**Player table prototype — what it does (v2 redesign, Phases 1–2):** runs locally (`npm install`, then `npm run dev`, at http://localhost:3000) as one screen with a **dark theme**; ~80 real-name players across QB/RB/WR/TE spanning a free-agent pool, the Rangoon Raccoons' roster, and 3 rival rosters; columns Owner, Player, **Pos (colored badge)**, Team, Kerf Ovr Rank, Kerf Pos Rank, Proj Points, Kerf Value, Ceiling, Edge (green +/red −), Market Value, Ovr ECR, Pos ECR, Dyn Ovr ECR, Dyn Pos ECR, Salary, Contract, tinted into **GartStats / Market / Contract Info** groups with a color-key legend; sorting with a filled caret; **FantasyPros-style tier bands** driven by six mock tier dimensions (bands appear only under a matching rank sort; the coupled sort↔position rules live in `lib/tierRules.ts`).

**Phase 2 (view system) adds:** a 3-way **roster toggle** (All / Rostered / Free Agents) beside a **Manager dropdown** (All / each team); the **position dropdown** (All/SuperFlex/Flex/QB/RB/WR/TE); a **column picker** to show/hide columns; **drag-to-reorder** column headers (@dnd-kit, mounted client-side to stay SSR-safe); and **saved views** — five built-in default views (Full, Auction Prep, Waivers, Trades, Start/Sit) plus user-created custom views, **persisted in browser localStorage**. Opens to Full. The table scrolls inside a bounded, sticky-header container so both scrollbars stay on screen. (Ovr ECR / Dyn Ovr ECR display the unique overall rank so tier bands stay contiguous.)

**Phase 3 adds:** a **Data Dictionary** overlay (button at the bottom) that defines each column — a one-line definition plus an expandable bulleted deep-dive (mechanics + source). **The structure is built but most content is placeholder** (flagged "Placeholder"), to be filled after data discovery (roadmap #2–3) and the engine (#4–6).

**What it deliberately does *not* do:** no real data (all values, ranks, and the six mock tier dimensions are hand-authored or derived, not computed by an engine); the data-dictionary content is mostly placeholder; ceilings still reset on reload (only view configs persist); no accounts, no deployment, no real data schema. DST has a badge color but no DST players exist in the mock data.

## Current limitations

- **Projection engine built; dollar layer not.** Kerf ranks/tiers and KERFUFFLE-scored Proj Points are live (#18); Kerf **Value**, Market Value, Edge, and ceiling suggestions stay "—" until the valuation layer (#20), which is gated behind the backtest (#19). The engine is a **manual step** — `npm run engine` after ingest — so the Kerf numbers are as fresh as the last engine run; re-run it after refreshing data.
- **Team defenses have no overall rank.** FantasyPros' superflex boards are offensive players only; DSTs carry positional rank + tier from the 1-QB board and sort last on overall-rank sorts (owner's call, D-12). Rostered-player ranking coverage is 162/170 either way.
- **Data freshness is manual.** The routine is `npm run archive` → `npm run ingest` → refresh; nothing is scheduled (deliberate — an expired-cookie scheduled run would silently collect nothing). The banner date is the tell.
- **Free agents come from the FantasyPros board, not CBS's FA page** (`/players` is JS-rendered; not yet captured). A player nobody rosters *and* FantasyPros doesn't rank isn't in the table; FA Proj Points are blank.
- **Three t7 players have blank salaries on CBS itself** (pre-auction oddity, likely pending assignment): stored as unknown, counted $0 toward the cap, warned on every ingest.
- **Transactions are in the database, not the UI** — all 60 real log entries (with inferred types; CBS has no type column) are queryable and lineage-tracked, but no screen shows them yet. FAB bid amounts remain unsolved upstream (recoverable via resulting salaries).
- **Dead-cap handling is built but untested against a live example** — zero exist pre-auction. Detection + storage per D-11 are unit-tested on synthetic fixtures; the first real cut should be spot-checked.
- **Limited persistence.** Custom *views* persist in browser localStorage (per browser, not synced); edited ceilings still reset on reload. The SQLite store persists league data; no accounts.
- **Local only.** The app is not deployed anywhere; it runs on the owner's machine via `npm run dev`.
- **Both data sources are now access-proven (read-only).** CBS via the session-cookie + HTML-parse method (spike #5); **FantasyPros via its official JSON API** (spike #7), and the CBS↔FantasyPros join is a direct `cbs_player_id` match. The **HOF cap-lift is now formally confirmed (issue #11):** ranking endpoints return the **full board** (draft PPR 521, HALF 883, etc., not the 10-of-520 preview), `tier` is **`premium`**, and the previously-`403` **projections / player-metadata / news** endpoints return data. ⚠ **Caveat:** `public_api_limited` still reports **`true`** even on HOF, so it must **not** be used as the tier signal (use row count + `tier`). **ADP is still `403`** — likely a wrong path/params, not a gate; nice-to-have, not blocking. See [`../fantasypros_data_discovery.md`](../fantasypros_data_discovery.md) §8.
- **Not every CBS page is in the static HTML (issue #11; transactions solved in #12).** Rosters, `/rules`, `/standings`, `/history` parse cleanly to static tables; `/transactions` turned out to paginate via **plain URL parameters** and is now fully captured and ingested. Still JS-rendered/unsolved: `/players` (the FA pool — why FAs derive from FantasyPros for now), `/draft/results` (auction values), `/scoring/live`. Also: the roster `Pos` column is the lineup **slot** (incl. FLEX/RB-WR-TE), not the player's position — ingestion reads the true position from the Players cell. See [`../cbs_data_discovery.md`](../cbs_data_discovery.md) §8.
- **KERFUFFLE scoring is now extracted (issue #11)** into committed structured form ([`../profiles/cbs_scoring_rules.json`](../profiles/cbs_scoring_rules.json)) — parsed from `/rules`, never hardcoded. ⚠ **CBS diverges from the written constitution** (defensive Int = 2 on CBS vs 3 in the constitution); **CBS is authoritative** for the engine. Dead-cap pseudo-rows: **0 in the profiled pre-auction snapshot** (detection rule confirmed: a roster row with a salary but no player id); **10 Practice-Squad players** across the league (ordinary player rows in a `Practice` section). This evidence resolved roadmap open decision #7 (owner, 2026-08-25 — see decision **D-11**), and the schema now implements it.
- **The raw archive is manual and current-season only.** `npm run archive` runs when the owner chooses (tied to the ~weekly CBS cookie refresh); nothing is scheduled — a scheduled task with an expired cookie would silently collect nothing, so automation is deliberately deferred (roadmap "Later"). **Historical CBS seasons are not captured** — the year switch isn't a URL parameter (still unsolved); the archive holds the current season only. One CBS page, `players-rankings`, returns a `302` redirect rather than content — archived verbatim as-is; a future parser should expect it.
- **Contract-length location resolved (spike #5): CBS exposes per-player contract length** (a "Contract" column, 1–4 yrs, on roster pages) — this closes roadmap open decision #1. Two things remain unsolved in CBS: programmatic **historical-season** retrieval (the year filter is JS/POST-driven) and **FAB winning-bid amounts** in the transaction log. See [`../cbs_data_discovery.md`](../cbs_data_discovery.md).
- Next.js 15 pulls in 3 high-severity npm advisories through build-time transitive deps (postcss; sharp, which we don't use). The only patch upgrades Next to v16, a breaking major-dependency change — deferred as a follow-up decision (see reality log). Negligible risk for a local, no-image prototype.

## Known bugs

| # | Bug | Impact | Severity | Status |
| --- | --- | --- | --- | --- |
| 1 | Tier bands duplicated/out-of-order + a React duplicate-key crash when sorting by Ovr ECR / Dyn Ovr ECR | Broken tier display on those sorts | High | **Fixed 2026-08-20** — those columns now sort by a unique derived rank (raw ECR had ties); band keys made collision-proof |
| 2 | Hydration mismatch console error from @dnd-kit (server vs client accessibility ids) | Console error on load; no functional break | Medium | **Fixed 2026-08-20** — drag context now mounts only on the client (SSR renders plain headers) |

## Build and deploy status

| | |
| --- | --- |
| **Active branch** | main — issue #18 (projection engine core) built locally, not yet committed/pushed |
| **Deployed to production** | No. Nothing is deployed anywhere. |
| **Environments live** | Local only — `npm run dev` at http://localhost:3000; operator commands: `npm run archive` (snapshot), `npm run ingest` (load), `npm run profile` (field guide) |
| **Tests** | **Vitest unit tests** (`npm test`) — **148 passing** (+33 for issue #18: **26 engine** — 21 pure-core (first-down rate derivation, **per-player empirical-Bayes shrinkage** incl. big-sample-keeps-own-rate / small-sample-pulled-to-prior / rookie-falls-to-position, first-down-aware scoring incl. **reconstruct-points-from-stored-components**, one-pool + positional rank derivation, deterministic Jenks tiering) + **5 DB-integration** (`runEngine` end to end on an in-memory store: the fp→cbs join, rate derivation, tier-calibration query, and engine_run/projection persistence); **5 projections-parser**; plus 2 data-layer wiring cases. Live: `npm run engine` scored 520 players, Josh Allen #1 overall) (20 app + 72 ingestion incl. **28 for issue #17**: historical CSV parsing/anchoring, advanced+standard FPTS-join, name-matching incl. DST-by-team + alias + **cross-position collision refusal**, DB-level idempotency + **whole-season replace**, TRUFFLE reference guard, and the scoring cross-check against real loaded data + 23 profiler): board derivation (unique-rank invariant on real data, real FP tiers, engine-null fields, loud bad-position failure), tier/sort/position rules, saved-views model, dictionary coverage; ingestion parsers (header-name mapping loudness, deliberate coercion, dead-cap classification/refusal) the FantasyPros mapper (board identity from the payload not the filename; **null stays null, never 0**; duplicate-CBS-id refusal), and **end-to-end synthetic-archive tests** (full ingest, DB-level idempotency, **$500-cap violation → loud rollback**, missing page/header rejection, double-rostered player, **latest-captured-wins ordering**); the profiler suite unchanged. `npm run build` clean; **live validation:** all 4 real archive runs ingested (572-player board), re-ingest idempotent, real-data render verified (banner, tiers, no MOCK). See [`../qa_test_plan.md`](../qa_test_plan.md). |

## Latest implementation summary

**2026-08-26 — KERFUFFLE projection engine core (issue #18, roadmap #8).** Built the first derived layer: FantasyPros projected stat lines → KERFUFFLE projected points → real Kerf ranks + tiers in the table. **Migration `005`** adds `projection_source` (normalized input — the FantasyPros projected stat line per player per pull, now parsed by `npm run ingest`; joined to CBS via `fp_player_id`, 520/528 offense matched), `engine_run` (a stamped run), and `projection` (derived per-player output). A **new offline step, `npm run engine`** (`tools/engine/run.mjs` + a pure, unit-tested `core.mjs`): derives **per-player first-down rates** from `player_season_stats` (2024+2025 pooled — receiving FD per reception, rushing FD per carry), **each shrunk toward its position rate by sample size** (empirical-Bayes; owner's "moderate" dial `rushK=75`/`recK=40`) so a player's own production drives his value where the sample supports it and rookies/thin samples fall back to the position average, **estimates** each projected player's rush/rec first downs and stores them as **distinct named components** with the applied/own/position rates + sample size for drill-down (the league's scoring edge, which FantasyPros doesn't project — D-14), **scores** the full line through the parsed `scoring_rule` config reusing #17's validated logic, and derives **Kerf overall/positional ranks** (one pool → superflex elevates QBs) and **gap-based tiers** (Jenks natural breaks, deterministic, calibrated to FantasyPros' tier counts on the superflex board per the owner's "consistent with FP" instruction — 16 overall bands). `lib/data/` joins the latest run onto the board, filling Kerf Ovr/Pos Rank + tiers and — per the owner — the **Proj Points** column with the KERFUFFLE-scored projection for every projected offensive player **including free agents** (CBS's own number kept behind the scenes). **Four owner decisions** taken in the pre-build Q&A: defenses render "—" for Kerf (offensive feed can't score defense); Proj Points shows our number for all offense; rates pool both seasons; tiers gap-based calibrated to FP. **Then, mid-build, the owner asked for player-specific first downs** (the edge) — added as empirical-Bayes shrinkage (above). **Dollars stay "—"** (the valuation layer #20, gated behind the backtest #19). **Validated:** 33 new unit tests (148 total — incl. a `runEngine` DB-integration test and the shrinkage math, both added on review/owner request), clean build, live `npm run engine` (520 players, best-QB overall rank = 1 — the superflex sanity check; Kyren Williams boosted on his real first-down rate), rendered-page check confirming real Kerf ranks/tiers with dollar columns still blank. No schema change beyond the additive migration; the app degrades gracefully to "—" if the engine has never run.

**2026-08-26 — Historical data storage (issue #17, roadmap #7).** Loaded the owner-provided historical exports into the store on a **separate ingestion path** (`npm run ingest:historical`), the data foundation the engine block depends on. **Migration `004`** adds three tables: `player_season_stats` (2024 & 2025 CBS stat lines — rushing/receiving/passing **first downs** + 2pt from the "Advanced" export joined with full volume from the "Standard" export + FPTS; **offense only**, **only players in our ~960 universe stored** — 864/season), `contract_history` (**2025** KERFUFFLE salaries — the only authoritative year per the owner; the full multi-year schedule kept verbatim for provenance only), and `auction_result` (TRUFFLE 2026, 69 rows, `is_reference=1`, **read by no consumer**). **Parsing:** the 3-row grouped CBS headers are positionally shifted, so columns are mapped by **anchored fixed index** — verified against Josh Allen (177 passing + 46 rushing first downs), Chase (73 receiving), and a per-player check that the two files agree on FPTS Total; any layout drift fails loudly. **Matching:** name→`cbs_player_id` on normalized name + position + team, with **team defenses matched by NFL team** and a curated alias map; **223/234** contract players matched (the 11 unmatched are dropped/retired players with no id in the current universe — stored with null id, named in the report, never dropped). **Scoring cross-check (the headline validation):** recomputing KERFUFFLE points from stat components using the parsed `scoring_rule` config lands on CBS's own FPTS Total for **~96% of players within 0.5 pt and 99.8% within 5 pt** (residuals are all small negatives — special-teams/return points the offensive export omits) — proving our stat data and scoring rules agree before the engine is built. Surfaced a scoring fact for #18: this league scores **no PPR receptions and no passing first downs**, only rush/rec first downs (1 pt). **Independent code + data-model review** then caught a real matcher bug — a name-only fallback that cross-matched different players sharing a name (Josh Johnson QB vs WR); fixed so the fallback respects position, and hardened with a loud same-id collision refusal, whole-season replace (no phantom rows on a re-match), 2025-salary-taken-only-from-the-'25-cell, and one whole-run transaction. **Validated:** 28 new unit tests (115 total), clean build, live full rebuild (`npm run ingest` → `npm run ingest:historical`) proven idempotent, real-data scoring cross-check re-confirmed on the corrected data. Docs-only change to `data/` layout: the 6 CSVs moved to `data/historical/` (git-ignored) with a provenance README. No UI change — this is plumbing for #18–#20.

**2026-08-25 — Storage schema + ingestion: the table is on real data (issue #12, roadmap #6).** Built the D-10 normalized store and put the app on it. **Schema:** `db/migrations/001` via `better-sqlite3` — `pull`, `fantasy_team`, `player`, `contract` (snapshot table; Practice Squad = a `roster_status`, dead cap = a team-level row with no player id, per owner decision **D-11**), `league_transaction`, `market_ranking`, `scoring_rule`, + the flat `board` read view. **Ingestion:** `npm run ingest` walks every archived run in date order inside one transaction per run — header-name column mapping (missing header = loud failure), deliberate per-field coercion (blank roster salary = real, stored null + warned), constitution invariants enforced loudly (12 teams, id-or-dead-cap, **cap ≤ $500 incl. IR**, contract years 1–4), idempotent upserts on natural keys, full `pull_id`/`fetched_at` lineage; a failure rolls the run back untouched. **Archiver extension (owner-approved):** the paginated transaction log is now fully captured (print-all + every page; 60 transactions vs 27 before) and a dynasty-STD probe added. **UI:** `lib/mockData.ts` deleted; the single data-access module (`lib/data/`) reads the board view; free agents derive from the FantasyPros draft-STD board (owner's display choice); engine columns render "—"; ceiling starts blank (seeds from Kerf Value when the engine exists); MOCK banner → "League data as of \<date\>"; DST is a real position; tier bands are FantasyPros' real tiers; default sort = Ovr ECR. **Validated:** 69 unit tests (incl. bad-fixture loud rejection + DB-level idempotency), clean build, live ingest of all 3 archive runs, real-data render check. Notable data realities: FP's dynasty board is scoring-agnostic and pre-season "ROS" returns the draft board (ingest trusts payload self-description and dedupes → 9 boards/pull); three t7 players have blank salaries on CBS itself.

**2026-08-25 — Source-profiling spike (issue #11, roadmap #5).** Built a discovery tool at [`tools/profile/`](../../tools/profile/) (`npm run profile`) that walks the latest raw archive (issue #10) and emits a **committed, shape-only** field profile to [`docs/profiles/`](../profiles/): `cbs_field_profile.json`, `fantasypros_field_profile.json`, the `/rules` scoring config **in full** (`cbs_scoring_rules.json`, 24 rules), and a readable `PROFILE.md` answering the spike's six questions. **Owner decision (question raised mid-build):** because the repo is **public**, player/roster/market values are masked to shape (`Ja'Marr Chase`→`Aa'Aaaa Aaaaa`); only non-private structural enums (positions, statuses, contract-year domain, bye weeks) list real values; league *rules* are committed in full. A **leak self-check** fails the run if any private field would publish a real value, and the sanitizer is unit-tested. **Findings:** all 12 rosters parse cleanly (170 players, 10 Practice-Squad, **0 dead-cap pseudo-rows** this pre-auction snapshot); scoring extracted with a **CBS-vs-constitution discrepancy caught** (defensive Int = 2 on CBS, not 3 — CBS authoritative); FantasyPros HOF **fully confirmed** (full board, `tier: premium`, projections/metadata/news unlocked) with the caveat that **`public_api_limited` still reads `true`** and must be ignored (ADP still `403`); and **several CBS pages are JS-rendered/paginated** (`/players`, `/transactions`, `/draft/results`, `/scoring/live`) — a correction to the optimistic page-level discovery. Added one dev-only dependency (`node-html-parser`). Discovery/docs only — no ingestion, schema, or database. Both discovery docs updated with corrected risk tables; evidence for the pseudo-row/Practice-Squad decision recorded in [`../data_model.md`](../data_model.md). 44 unit tests pass; build clean; validated end-to-end against the real archive (0 leaks over 451 fields).

**2026-08-25 — Raw snapshot archival tool (issue #10, roadmap #4).** Promoted the throwaway spike pulls into a durable, repeatable operator tool at [`tools/archive/`](../../tools/archive/) — `capture.mjs` (the archiver), `check-cookie.mjs` (promoted cookie checker), `shared.mjs`, and a how-to-run `README.md`, wired to `npm run archive` and `npm run archive:check-cookie`. Each run writes every fetched response **verbatim** into a new dated folder `data/raw/{timestamp}/{cbs|fantasypros}/…` plus a per-run `manifest.json` (source, URL, fetched_at, HTTP status). **Append-only** — a fresh folder every run, nothing overwritten. **No parsing, no normalization, no database** (all deferred to issues #11/#12). CBS capture is the spike's page set with the **roster report expanded to all 12 teams** (the spike pulled only teams 1–2); FantasyPros runs the existing probe set on the **HOF key**. Credentials are read from the **existing spike `.env` files** (owner's choice — no re-paste); `data/` and all credentials are **git-ignored**. Validated by **two live runs**: two dated folders, run 1 byte-identical before/after run 2 (append-only proven), all 12 CBS rosters returned `200`, and FantasyPros returned **~520-row** payloads with projections/metadata unlocked on HOF (ADP still `403`; `players-rankings` returns a `302`, both archived as-is). One follow-up recorded for the owner: delete the now-superseded spike `pull.mjs` scripts once the tool is trusted — **without** removing the spike `.env` files this tool depends on (see [`roadmap.md`](roadmap.md) and the reality log). Nothing was ever written to CBS or FantasyPros.

**2026-08-20 — FantasyPros data discovery spike (issue #7, roadmap #3).** Proved, against the live FantasyPros JSON API, that we can pull expert-consensus rankings read-only — and settled the roadmap's "expected ugliest part" (matching players across the two sources) in the best possible way. Access is a self-serve `x-api-key` (the API is **not** approval-gated as the roadmap assumed). Rankings return **ECR (`rank_ecr`), positional rank (`pos_rank`), tiers, and the expert spread (`rank_min/max/ave/std`)** across **redraft + dynasty + ROS + weekly** and **PPR / half / standard**, from 99 experts. **The join is a direct id match: FantasyPros publishes a `cbs_player_id` on every player** that equals CBS's own id — confirmed against real CBS ids (Chase `2966320`, Nacua `3121687`, McCaffrey `2136743`). Cost/limit: the **free** tier is a **top-10-of-520 preview** (`public_api_limited: true`) and blocks projections/metadata/ADP/news (`403`); the full board needs the **HOF tier (~$9/mo)** — the owner upgraded, but the cap-lift + endpoint unlock **still need a confirming re-run** (the key had not yet propagated to HOF at write-time). Deliverables: a discovery report ([`../fantasypros_data_discovery.md`](../fantasypros_data_discovery.md)) and throwaway read-only tooling in `spikes/fantasypros-api/` (`pull.mjs`, `match.mjs`; not part of the app). No product code, schema, or app change. Resolves roadmap open decision #2 ([`../decision_log.md`](../decision_log.md) D-09). Nothing was ever written to FantasyPros; the API key and pulled data are git-ignored.

**2026-08-20 — CBS data discovery spike (issue #5, roadmap #2).** Proved, against the real KERFUFFLE league, that we can pull live data read-only. The old CBS v3 JSON API is dead; the working path is **authenticated HTML fetch + parse** — the modern league site renders data into page tables, gated only by the owner's **session cookie**. Reachable read-only: every team's roster (`/teams/roster-report/{teamId}/1`, teams 1–12) with **salary and contract length**, the free-agent pool (`/players`), the transaction log (`/transactions`, filterable 2024–26), league scoring/settings (`/rules`), and draft/auction values (`/draft/results`). **Contract length is present** (closes open decision #1); players carry a stable **CBS numeric id** (the FantasyPros join key). Validated by parsing the owner's real roster and confirming the numbers with him. Deliverables: a discovery report ([`../cbs_data_discovery.md`](../cbs_data_discovery.md)) and throwaway read-only tooling in `spikes/cbs-api/` (not part of the app). No product code, schema, or app change. Unsolved follow-ups: historical-season fetch and FAB bid amounts. Nothing was ever written to CBS; the cookie and pulled data are git-ignored.

**2026-08-20 — Table redesign, Phase 3 of 3 (data dictionary shell).** Added the Data Dictionary overlay: a button at the bottom opens a modal defining each column — a one-line definition (kept under 15 words) plus an expandable bulleted deep-dive (mechanics + source). Content is intentionally **mostly placeholder** (flagged per field) pending data discovery and the engine; the structure (`lib/dataDictionary.ts` + `components/DataDictionary.tsx`) is stable so later issues just fill in text. A coverage unit test guarantees every column has an entry (21 tests total). This completes the 3-phase redesign. Validated: unit tests + clean build + rendered-DOM check.

**2026-08-20 — Phase 2 polish: filter model, two bug fixes, sticky scroll.** On owner review: the roster control became a 3-way toggle (All / Rostered / Free Agents) next to a renamed **Manager** dropdown (All / team). Fixed two bugs the owner found — broken/duplicated **tier bands** when sorting by the overall-ECR columns (they sorted by raw ECR, which had ties; now they sort by a unique derived rank, and band keys are collision-proof) and a **@dnd-kit hydration** console error (drag now mounts client-side only). Table now scrolls in a bounded container with a **sticky header** so scrollbars stay on screen. 19 unit tests (added the unique-rank invariant). Validated: unit tests + clean build + rendered-DOM checks (SSR headers confirmed drag-free).

**2026-08-20 — Table redesign, Phase 2 of 3 (the view system).** Added the interactive view layer: the roster filter became a dropdown (All Players / Free Agents / each team) with a separate "include free agents" toggle; a column picker (show/hide); drag-to-reorder column headers via **@dnd-kit**; and **saved views** — five built-in default views mirroring the user flows (Full, Auction Prep, Waivers, Trades, Start/Sit) plus user-created custom views, persisted in **browser localStorage** (D-05). Opens to Full. Two new dependencies (@dnd-kit, plus the earlier Vitest). Validated: 18 unit tests (added the views model) + clean build + rendered-DOM checks. Phase 3 (data-dictionary popup) is next. Deferred by owner: ceiling persistence (still resets).

**2026-08-20 — Table redesign, Phase 1 of 3 (dark theme + columns + tier bands).** On owner feedback, the prototype was redesigned toward a dark, Gamecast-style dashboard (teal accent, compact rows, centered "Gart Dash" title). Position cells became colored badges (QB/RB/WR/TE/DST). The single tier column was replaced by **FantasyPros-style tier bands** driven by six mock tier dimensions (Kerf/ECR/Dynasty × overall/positional); bands appear only under a matching rank sort, with the coupled sort↔position behaviour (auto-switch to QB, revert-to-no-tiers) centralized and unit-tested in `lib/tierRules.ts`. Columns were expanded/renamed (Kerf Ovr + Pos Rank; Ovr/Pos ECR; Dyn Ovr/Pos ECR; Market Value; Proj Points), regrouped into GartStats/Market/Contract-Info tints with a color key, and Edge made plain. A position dropdown (incl. SuperFlex/Flex) was added. **Vitest** was introduced (14 tests). This is Phase 1; the filter-bar/view system (Phase 2) and data dictionary (Phase 3) are not yet built. Validated by unit tests + clean build + rendered-DOM checks.

**2026-08-19 — Player table prototype built (Issue #1, UI only, mock data).** The first product code landed: a single-screen Next.js (App Router) + TypeScript app, styled with Tailwind, table powered by TanStack Table v8, on the stack decided in [`../decision_log.md`](../decision_log.md) D-01. It renders 79 hand-authored mock players across QB/RB/WR/TE (free-agent pool + Rangoon Raccoons + 3 rivals) with the full column set, grouped "Yours" vs "The Market" columns and a color-coded Edge column, sorting, combining roster + position filters, colored tier badges, an inline-editable Ceiling pre-seeded to KERF Value, and a permanent MOCK-DATA banner. Runs locally with `npm install` then `npm run dev`. All data is mock; no engine, no schema, no persistence, no deployment. `npm run build` passes; interactive QA is manual (owner sign-off pending) — see [`../qa_test_plan.md`](../qa_test_plan.md). After an owner review the same day, two shared-table columns (KERF Rank, Proj Pts) were added to serve the non-auction flows, and the per-lens fields (waiver bid range, remaining cap, trade compare, matchup) were logged to [`../feature_backlog.md`](../feature_backlog.md). Styling was then refactored onto a semantic design-token layer (all colors defined once in `tailwind.config.ts`, components reference role-based tokens) — no visual change; see [`../decision_log.md`](../decision_log.md) D-02.

**2026-08-19 — Project initialized, docs only; stack decided, Issue #1 opened.** The four intent docs (vision, brief, user flows, roadmap) were written and committed. The tech stack was then decided (Next.js + TypeScript — [`../decision_log.md`](../decision_log.md) D-01) and GitHub Issue #1 (player table prototype, UI only) was created and readied for build.

---

**Related docs:** [`roadmap.md`](roadmap.md) is what we plan to build. [`implementation_reality_log.md`](implementation_reality_log.md) is why what we built differs from what we planned.